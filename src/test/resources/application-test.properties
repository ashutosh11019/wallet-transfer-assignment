# Use a shared in-memory SQLite DB for tests so runs are isolated and don't touch wallet.db.
# cache=shared lets the connection pool's multiple connections share the same in-memory instance.
spring.datasource.url=jdbc:sqlite:file::memory:?cache=shared&foreign_keys=on
